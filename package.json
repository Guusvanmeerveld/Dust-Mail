{
	"name": "@dust-mail/root",
	"private": true,
	"license": "MIT",
	"version": "0.1.4",
	"author": {
		"name": "Guus van Meerveld"
	},
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"scripts": {
		"format": "prettier apps/client/src packages/autodiscover/src --write",
		"build": "turbo run build",
		"lint": "turbo run lint",
		"test": "turbo run test",
		"deploy": "turbo run deploy",
		"dev": "turbo run dev",
		"clean": "npx -y rimraf node_modules packages/**/node_modules/ apps/**/node_modules/ pnpm-lock.yaml && pnpm install",
		"postinstall": "husky install",
		"version": "conventional-changelog -p angular -i CHANGELOG.md -s && git add CHANGELOG.md",
		"postversion": "git push origin"
	},
	"devDependencies": {
		"@trivago/prettier-plugin-sort-imports": "^3.2.0",
		"commitizen": "^4.2.5",
		"conventional-changelog-cli": "^2.2.2",
		"cz-conventional-changelog": "3.3.0",
		"husky": "^8.0.1",
		"prettier": "^2.6.2",
		"rimraf": "^3.0.2",
		"turbo": "^1.3.4"
	},
	"dependencies": {},
	"packageManager": "pnpm@7.5.2",
	"config": {
		"commitizen": {
			"path": "./node_modules/cz-conventional-changelog"
		}
	}
}
